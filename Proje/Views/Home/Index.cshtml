@{
    ViewData["Title"] = "Home Page";
}
@model List<Proje.Models.departmanlar>
<h2 class="text-info text-center">Görev ve Detaylar</h2>



                <hr />
@* @foreach (var item in Model)
{
    <div>@item.Id</div>
    <div>@item.Name</div>
    <div>@item.Description</div>
    <br />
} *@


<table class="table table-hover mt-5 shadow-lg p-3 mb-5 bg-white rounded">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Görev</th>
            <th scope="col">Detaylar</th>

            <th scope="col">Kime Ait</th>
            <th scope="col">Güncelle</th>

            <th scope="col">Sil</th>
        </tr>
    </thead>
    <tbody>
     @foreach (var item in Model)
{

       <tr>
      <td>@item.Id</td>
      <td>@item.departmanad</td>
                <td>@item.Detay</td>
                <td>@item.GorevAdami</td>
                <td><a class="btn btn-success" href="/Home/DepartmanGetir/@item.Id">Güncelle</a></td>

    <td><a class="btn btn-danger" href="/Home/DepartmanSil/@item.Id">Görevi sil</a></td>
        </tr>
} 


    </tbody>
</table>
<td><a class="btn btn-secondary" href="/Home/YeniDepartman">Yeni Görevler Ekle</a></td>
